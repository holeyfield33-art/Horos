{
  "name": "@horos/router",
  "version": "0.1.0",
  "description": "Horos — deterministic context router with signed, replayable provenance receipts.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.8.2",
    "vitest": "^2.1.8"
  }
}
